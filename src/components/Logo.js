import React from "react";
import burgerlogo from "../assets/kisspng-hamburger.png";

const Logo = props => (
    <div className="logo-top">
        <img src={burgerlogo} alt="number 1 burger" />
    </div>
);

export default Logo;
